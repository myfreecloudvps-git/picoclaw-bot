services:
  - type: web
    name: picoclaw-bot
    runtime: python
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn picoclaw.bot:app
    envVars:
      - key: TELEGRAM_TOKEN
        sync: false
      - key: PYTHON_VERSION
        value: 3.11.6
